/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['weblearnbd.net', 'lh3.googleusercontent.com', 'images.unsplash.com']
    }
}

module.exports = nextConfig
